#!/usr/bin/env python
import sys
import subprocess

# Run the license year update script
result = subprocess.run([sys.executable, "scripts/update_license_year.py"])
if result.returncode != 0:
    sys.exit(result.returncode)

# Stage LICENSE.md if it changed
subprocess.run(["git", "add", "LICENSE.md"])
